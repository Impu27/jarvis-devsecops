# JARVIS RAG Microservice — Python Dependencies
# Verified working: April 2026
#
# KEY RULES:
#   - fastapi MUST be 0.115.9        (chromadb 1.0.9 hard-requires it)
#   - pydantic NOT pinned            (ollama>=0.4.8 needs >=2.9; let pip pick)
#   - python-multipart REQUIRED      (FastAPI File/Form uploads won't work without it)

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.115.9
uvicorn[standard]==0.30.1
python-multipart>=0.0.9   # Required by FastAPI for File(...) / Form(...) endpoints

# ── Pydantic — intentionally unpinned, pip resolves to >=2.9 ─────────────────
pydantic>=2.9,<3.0.0

# ── LangChain RAG stack ───────────────────────────────────────────────────────
langchain==0.3.25
langchain-core==0.3.83
langchain-community==0.3.23
langchain-ollama==0.3.3
langchain-huggingface==0.1.2
langchain-chroma==0.2.4
langchain-text-splitters==0.3.8

# ── Vector DB & embeddings ────────────────────────────────────────────────────
chromadb==1.0.9
sentence-transformers==4.1.0

# ── PDF parsing ───────────────────────────────────────────────────────────────
pypdf==5.5.0

# ── HTTP client ───────────────────────────────────────────────────────────────
httpx==0.27.0